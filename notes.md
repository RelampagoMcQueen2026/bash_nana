
**Comand line interface (CLI)**
Run commands to tell computers what to do
A way to interact with a computer program by typing text commands

The program to run & interpet the commands is call **SHELL**

*BASH* is one flavor of a **shell**, most commond implementation of a shell in Linux systems

 Bash is also full programming language to automate task

 Bash scripting == shell scripting

 only difference is that bash has more feature than other shells

 **Terminal** is a graphical windows to type command or run scripts

 On windows use WSL


How does the interpreter know which shell program the script is written in when it executes

Shebang statement
Is the character sequene at the very top of a script file that tells the OS which interpreter to use to run the file

/bin/bash
where the program is located


shell script is a text file with a bunch of commands


for PATTERN in ${ERROR_PATTERNS[@]}; do
Array subscript that means each element remains a separate entity

**My comment to test commits**
